{
  "compilerOptions": {
    "strict": true,
    "lib": ["deno.ns", "deno.window"]
  },
  "imports": {
    "hono": "jsr:@hono/hono@^4",
    "hono/deno": "jsr:@hono/hono@^4/deno",
    "hono/": "jsr:@hono/hono@^4/",
    "@std/path": "jsr:@std/path@^1",
    "@std/streams": "jsr:@std/streams@^1",
    "@std/cli": "jsr:@std/cli@^1",
    "@std/assert": "jsr:@std/assert@^1"
  },
  "tasks": {
    "dev": "PROJECTS_ROOT=${PROJECTS_ROOT:-$HOME/Projects} deno run --watch --allow-read=$HOME/.claude,${PROJECTS_ROOT:-$HOME/Projects},. --allow-write=${PROJECTS_ROOT:-$HOME/Projects} --allow-net=127.0.0.1:3456 --allow-env=HOME,PROJECTS_ROOT --allow-run=osascript,open,git --deny-write=$HOME/.claude main.ts",
    "start": "PROJECTS_ROOT=${PROJECTS_ROOT:-$HOME/Projects} deno run --allow-read=$HOME/.claude,${PROJECTS_ROOT:-$HOME/Projects},. --allow-write=${PROJECTS_ROOT:-$HOME/Projects} --allow-run=claude,git,osascript,open --allow-net=127.0.0.1:3456 --allow-env=HOME,PROJECTS_ROOT --deny-write=$HOME/.claude main.ts",
    "check": "deno check main.ts",
    "test": "deno test --allow-read=$HOME/.claude,.,/var/folders --allow-env=HOME --allow-write=/var/folders --allow-run=git tests/"
  },
  "fmt": {
    "lineWidth": 100,
    "indentWidth": 2,
    "singleQuote": false
  },
  "lint": {
    "rules": {
      "tags": ["recommended"]
    }
  }
}
